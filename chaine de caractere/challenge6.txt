#include <stdio.h>
#include <string.h>

int main() {
    
    char tab[] = "abacadgaeagfaa";
    char a ='a';
    int count = 0;
    for(int i = 0; i < tab[i] - 1;i++){
        if(a == tab[i]){
             count++;
            
        }
         
    }
    
    printf("%d",count); 
    return 0;
}